{
  "dependencies": {
    "@frugal-ui/base": "^1.0.0-beta28",
    "@frugal-ui/fds": "^0.0.1-beta2"
  },
  "scripts": {
    "build": "esbuild src/index.ts --format=iife --global-name=APP --bundle --outdir=dist --loader:.woff=file --loader:.woff2=file --loader:.ttf=file --loader:.otf=file"
  }
}